# Demo prompt

Use this exact prompt in a new GitHub Copilot Chat for both runs:

> Implement the retry policy for failed actions and add comprehensive unit tests.
> Make the change in this project and run the tests.

Do not add business rules to the prompt. They are deliberately repository knowledge
that should come from `.github/copilot-instructions.md`.
